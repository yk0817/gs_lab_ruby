require './common'

# idを指定
# p Headline.find(1)
# idの範囲指定
# headlines = Headline.where(id: [1..10])
# テーブル先頭のデータを取得
# p Headline.first
# テーブル最後のデータを取得
# p Headline.last
#
# p Headline.where(id: 1..10)
# データ更新
# Headline.find(1).update(title: 'test更新')
# データ削除
# Headline.first.destroy
